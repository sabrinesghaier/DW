
{% extends 'navbar.html.twig' %}
{% form_theme Formulaire 'bootstrap_4_layout.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
<link href="{{asset('/css/style.css')}}" rel="stylesheet" media="all">  
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
 
{% endblock %}
{% block contenuPrincipal %}
    {{ parent() }}

<body class="bg-light">
<div class="container">    
    <div class="row">

     <div class="col-6  mb-5">

                <img class="logo" src="{{asset('/images/64.png')}}">
            </div>
       
            

            <div class="col-6 mb-5">
                <h1 class="h1 m-4  font-weight-normal">Messagerie</h1>
                <div class="bg-light p-4 ">
                    {{ form_start(Formulaire) }}
                    
                   
                    <div class="form-group">
                        {{ form_row(Formulaire.nom) }}
                    </div>
                     <div class="form-group">
                        {{ form_row(Formulaire.objet) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(Formulaire.email) }}
                    </div>
                    
                  
                   
                    <div class="form-group">
                        {{ form_row(Formulaire.texte) }}
                    </div>
                     <div class="form-group">
                    {{ form_row(Formulaire.photo) }}
                    
                    </div>
                   
                        {{form_widget(Formulaire.envoyer, { 'value': '' }) }}
           
               {{form_end(Formulaire) }}
             
           
            </div>
           </div>
        </div>

   
</div>

</body>







{% endblock %}    


